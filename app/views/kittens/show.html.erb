<h1>Kitten Details</h1>

<div>
    <p><strong>Name:</strong> <%= @kitten.name %></p>
    <p><strong>Age:</strong> <%= @kitten.age %></p>
    <p><strong>Cuteness:</strong> <%= @kitten.cuteness %></p>
    <p><strong>Softness:</strong> <%= @kitten.softness %></p>
</div>

<%# <%= button_to 'Delete Kitten', kitten_path(@kitten), method: :delete, data: { confirm: 'Are you sure?' } %> 
<%= link_to 'Delete Kitten', kitten_path(@kitten), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>

<%= link_to 'Edit', edit_kitten_path(@kitten) %>
<%= link_to 'Back', kittens_path %>